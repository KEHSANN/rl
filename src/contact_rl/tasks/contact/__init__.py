"""Contact-explicit multi-gait locomotion task (Omar & Khadiv, arXiv:2510.03599v2).

Importing this package's ``config`` sub-packages (done automatically by
``contact_rl.tasks`` via ``import_packages``) registers the concrete robot tasks
with mjlab's task registry.
"""
